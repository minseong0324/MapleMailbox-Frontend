import styled from 'styled-components';

const KakaoLoginButton = styled.img`
  position: relative;
  z-index: 2;
  width: 250px;
  height: 40px;
`;

const KakaoWrapper = styled.div`
  position: relative;
  z-index: 2;

`;

export const s = {
    KakaoLoginButton,
    KakaoWrapper
}