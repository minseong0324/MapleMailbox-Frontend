import styled from 'styled-components';

const NaverLoginButton = styled.img`
  position: relative;
  z-index: 2;
  width: 250px;
  height: 40px;
`;

const NaverWrapper = styled.div`
  position: relative;
  z-index: 2;

`;

export const s = {
    NaverLoginButton,
    NaverWrapper
}