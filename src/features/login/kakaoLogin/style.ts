import styled from 'styled-components';

const KakaoLoginButton = styled.img`
  position: relative;
  z-index: 2;
  width: 160px;
  height: 40px;
`;

const KakaoWrapper = styled.div`
  position: relative;
  z-index: 2;

`;

export const s = {
    KakaoLoginButton,
    KakaoWrapper
}